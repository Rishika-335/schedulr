# ── Database ──────────────────────────────────────────────────────────────────
# Aiven MySQL (free tier) — get this from your Aiven console
DATABASE_URL=mysql+pymysql://avnadmin:YOUR_PASSWORD_HERE@mysql-2c4ea88-devjotsingh50-95cc.e.aivencloud.com:27117/defaultdb

# ── App ───────────────────────────────────────────────────────────────────────
SECRET_KEY=schedulr-secret
DEBUG=true
APP_URL=http://localhost:5173

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=["*"]

# ── Email (optional — booking confirmations) ──────────────────────────────────
EMAIL_ENABLED=false
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@schedulr.app
